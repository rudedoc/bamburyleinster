<h1>Listing feed_backs</h1>

<table>
  <thead>
    <tr>
      <th>Email</th>
      <th>Phone number</th>
      <th>Call back</th>
      <th>Content</th>
      <th>Slip number</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @feed_backs.each do |feed_back| %>
      <tr>
        <td><%= feed_back.email %></td>
        <td><%= feed_back.phone_number %></td>
        <td><%= feed_back.call_back %></td>
        <td><%= feed_back.content %></td>
        <td><%= feed_back.slip_number %></td>
        <td><%= link_to 'Show', feed_back %></td>
        <td><%= link_to 'Edit', edit_feed_back_path(feed_back) %></td>
        <td><%= link_to 'Destroy', feed_back, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Feed back', new_feed_back_path %>
